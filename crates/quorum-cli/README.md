# quorum-cli

The `quorum` binary: command parsing, output, TUI, hook installer.

User-facing documentation, install instructions, and security notes live
in the [repository root README](https://github.com/accillion/quorum#readme).
Install via `cargo install quorum-cli` produces a binary named `quorum`.

License: Apache-2.0.
